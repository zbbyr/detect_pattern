注意事项：
1、在 detect_pattern.py 同目录下存放待判定文件 detect_pattern.csv 
2、detect_pattern.csv 的包含且只能包含五列，分别为“手表编号”、“接收时间”、“校内天线信号强度”、“校外天线信号强度”、“信号强度差值”
3、如果传入的数据为空会报错
4、如果传入数据包含多个手表，会报错
5、判定结果记录在 result.csv 中，这个文件也必须在此目录下，如果没有程序会自动创建并初始化
6、每计算一个结果，会添加到 result.csv 的后面，不会覆盖之前的结果
7、result.csv 包含三列，分别为“手表编号”、“接收时间”、“状态”，其中“接收时间”存放的是传入的最后一条数据的时间

如何使用脚本：
命令行运行 python detect_pattern.py（注意使用python3，python2可能会报错）
